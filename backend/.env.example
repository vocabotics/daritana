# Server Configuration
NODE_ENV=development
PORT=7001
HOST=localhost
FRONTEND_URL=http://localhost:7000

# Database Configuration
DATABASE_URL="postgresql://user:password@localhost:5432/daritana_dev"

# JWT Configuration (use strong secrets in production!)
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-min-32-chars-change-in-production
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Email Configuration
SENDGRID_API_KEY=your_sendgrid_api_key_here
FROM_EMAIL=noreply@daritana.com
FROM_NAME=Daritana Platform

# CORS Configuration
CORS_ORIGIN=http://localhost:7000,http://127.0.0.1:7000,http://localhost:5174,http://127.0.0.1:5174
